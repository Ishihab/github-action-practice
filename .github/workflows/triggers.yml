on:
  push:
    branches: 
      - main
      - test

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
          - name: checkout code
            uses: actions/checkout@v5
          - name: run a one-line script
            run : echo "This workflow  is triggers on ${{ github.event_name}} even on branche ${{ github.ref_name}} by ${{ github.actor}}"
        